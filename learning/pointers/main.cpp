int* ptr; // create pointer
int var = 7;
int foo = 21;
ptr = &var; // assign to ptr the address of var

//ptr =   // the value is the address of the thing it points to, not the value itself

//*ptr = // the value that the pointer is pointing to

//refernce

int & ref = var; // create a refernce

/*
pointers can be reassigned but refernces cannot
*/